- title = title || 'Login Error'
doctype html
html
	head
		title #{title}
		include includes/materialize_head.pug
	body
		include includes/header.pug
		main.container
			p login error
			p Go back or #[a(href='/login') try again].
		include includes/footer.pug
